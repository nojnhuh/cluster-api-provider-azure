---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: asomanagedmachinepools.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: ASOManagedMachinePool
    listKind: ASOManagedMachinePoolList
    plural: asomanagedmachinepools
    singular: asomanagedmachinepool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ASOManagedMachinePool is the Schema for the asomanagedmachinepools
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ASOManagedMachinePoolSpec defines the desired state of ASOManagedMachinePool
            properties:
              providerIDList:
                description: ProviderIDList is the list of cloud provider IDs for
                  the instances.
                type: string
            type: object
          status:
            description: ASOManagedMachinePoolStatus defines the observed state of
              ASOManagedMachinePool
            properties:
              ready:
                description: |-
                  Ready represents whether or not the infrastructure is ready to be used. It fulfills Cluster API's
                  machine pool infrastructure provider contract.
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
