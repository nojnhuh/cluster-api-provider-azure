---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: asomanagedclusters.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: ASOManagedCluster
    listKind: ASOManagedClusterList
    plural: asomanagedclusters
    singular: asomanagedcluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ASOManagedCluster is the Schema for the asomanagedclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ASOManagedClusterSpec defines the desired state of ASOManagedCluster
            properties:
              controlPlaneEndpoint:
                description: |-
                  ControlPlaneEndpoint is the location of the API server within the control plane. It fulfills Cluster
                  API's cluster infrastructure provider contract.
                properties:
                  host:
                    description: The hostname on which the API server is serving.
                    type: string
                  port:
                    description: The port on which the API server is serving.
                    format: int32
                    type: integer
                required:
                - host
                - port
                type: object
              resources:
                description: Resources are embedded ASO resources to be managed by
                  this resource.
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                type: array
            type: object
          status:
            description: ASOManagedClusterStatus defines the observed state of ASOManagedCluster
            properties:
              ready:
                description: |-
                  Ready represents whether or not the cluster has been provisioned and is ready. It fulfills Cluster
                  API's cluster infrastructure provider contract.
                type: boolean
              resources:
                description: Resources represents the status of the resources defined
                  in the spec.
                items:
                  description: ResourceStatus represents the status of a resource.
                  properties:
                    group:
                      type: string
                    kind:
                      type: string
                    message:
                      description: Message gives more information as to why a resource
                        is not ready.
                      type: string
                    name:
                      type: string
                    ready:
                      description: Ready reflects a Ready condition on the resource,
                        if one is defined.
                      type: boolean
                    version:
                      type: string
                  required:
                  - group
                  - kind
                  - name
                  - version
                  type: object
                type: array
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
