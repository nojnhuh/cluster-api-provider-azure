---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: azureasomachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: AzureASOMachine
    listKind: AzureASOMachineList
    plural: azureasomachines
    singular: azureasomachine
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AzureASOMachine implements the azureasomachine API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AzureASOMachineSpec defines the desired state of AzureASOMachine.
            properties:
              patches:
                description: |-
                  Patches are applied to Resources before they are created or updated.

                  Data passed to JSONPatchValueFrom templates is a map consisting of the following keys:

                  - self: this AzureASOMachine
                  - machine: the owning Machine resource
                  - cluster: the associated Cluster resource
                  - bootstrapData: the Secret referred to by the owning Machine's spec.bootstrap.dataSecretName

                  e.g. a template could be defined in YAML as

                      template: '{{ .self.metadata.name }}'
                items:
                  description: |-
                    ResourcesPatch defines an ordered list of patches to apply to
                    resources matching a set of selection criteria.
                  properties:
                    jsonPatches:
                      description: JSONPatches defines the patches to be applied,
                        in order.
                      items:
                        description: |-
                          JSONPatch represents a JSON Patch defined by [RFC 6902].

                          [RFC 6902]: https://tools.ietf.org/html/rfc6902
                        properties:
                          from:
                            maxLength: 256
                            type: string
                          op:
                            description: |-
                              JSONPatchOp is the `op` of a [RFC 6902] JSON patch.

                              [RFC 6902]: https://tools.ietf.org/html/rfc6902
                            enum:
                            - add
                            - remove
                            - replace
                            - move
                            - copy
                            - test
                            type: string
                          path:
                            maxLength: 256
                            type: string
                          value:
                            description: |-
                              Value is a literal value used per RFC 6902.
                              Only one of Value and ValueFrom may be set.
                            x-kubernetes-preserve-unknown-fields: true
                          valueFrom:
                            description: |-
                              ValueFrom defines the value of the patch.
                              Only one of Value and ValueFrom may be set.
                            properties:
                              template:
                                description: |-
                                  Template is the Go template to be used to calculate the value.
                                  The template must evaluate to a valid YAML or JSON value.
                                maxLength: 1024
                                minLength: 1
                                type: string
                            required:
                            - template
                            type: object
                        required:
                        - op
                        - path
                        type: object
                      maxItems: 32
                      type: array
                    selectors:
                      description: |-
                        Selectors declare conditions to match resources.
                        A resource is matched when at least one selector matches.
                      items:
                        description: ResourcesPatchSelector filters Resources to which
                          patches should apply.
                        properties:
                          apiVersion:
                            description: |-
                              APIVersion, when defined, matches the apiVersion of the resource exactly.
                              Otherwise matches all APIVersions.
                            type: string
                          kind:
                            description: |-
                              Kind, when defined, matches the kind of the resource exactly.
                              Otherwise matches all kinds.
                            type: string
                          name:
                            description: |-
                              Name, when defined, matches the metadata.name of the resource exactly.
                              Otherwise matches all names.
                            type: string
                        type: object
                      maxItems: 32
                      type: array
                  type: object
                maxItems: 32
                type: array
              providerID:
                description: |-
                  ProviderID is the unique identifier as specified by the cloud provider.
                  It fulfills Cluster API's machine infrastructure provider contract.
                type: string
              providerIDSource:
                description: |-
                  ProviderIDSource defines where the Machine's providerID can be found.
                  When set, CAPZ takes ownership of setting spec.providerID, even if
                  already set by the user.
                properties:
                  configMap:
                    description: ConfigMapReference refers to a particular value in
                      a ConfigMap.
                    properties:
                      key:
                        description: Key is the key of the ConfigMap's data.
                        properties:
                          template:
                            description: |-
                              Template is a Go text/template that evaluates to a string.

                              Data passed to templates is a map consisting of the following keys:

                              - self: this CAPZ resource

                              e.g. a template could be defined in YAML as

                                  template: '{{ .self.metadata.name }}'
                            type: string
                          value:
                            description: Value is a literal string.
                            type: string
                        type: object
                      name:
                        description: Name is the metadata.name of the ConfigMap.
                        properties:
                          template:
                            description: |-
                              Template is a Go text/template that evaluates to a string.

                              Data passed to templates is a map consisting of the following keys:

                              - self: this CAPZ resource

                              e.g. a template could be defined in YAML as

                                  template: '{{ .self.metadata.name }}'
                            type: string
                          value:
                            description: Value is a literal string.
                            type: string
                        type: object
                    required:
                    - key
                    - name
                    type: object
                type: object
              resources:
                description: Resources are embedded ASO resources to be managed by
                  this resource.
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                maxItems: 32
                type: array
            type: object
          status:
            description: AzureASOMachineStatus defines the observed state of AzureASOMachine.
            properties:
              resources:
                items:
                  description: ResourceStatus represents the status of a resource.
                  properties:
                    ready:
                      type: boolean
                    resource:
                      description: StatusResource is a handle to a resource.
                      properties:
                        group:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                        version:
                          type: string
                      required:
                      - group
                      - kind
                      - name
                      - version
                      type: object
                  required:
                  - ready
                  - resource
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
